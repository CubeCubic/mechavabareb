# 🎓 СИСТЕМА РЕКОМЕНДАЦИЙ УНИВЕРСИТЕТОВ ГРУЗИИ
## Полное руководство для начинающих

---

## 📚 ЧТО ЭТО?

Это веб-сайт, который помогает абитуриентам найти подходящие программы обучения в университетах Грузии на основе их баллов по экзаменам и предпочтений.

**Что делает система:**
1. Абитуриент выбирает город, тип вуза, направление обучения
2. Вводит свои баллы по экзаменам
3. Система показывает ТОП подходящих программ с процентом совместимости и шансами поступления

---

## 📁 СТРУКТУРА ПРОЕКТА

Ваш проект состоит из следующих файлов:

```
university-recommendation/
│
├── app.py                      # Главный файл Flask приложения
├── recommendation_system.py    # Логика рекомендаций (алгоритм)
├── universities_info.py        # Справочная информация
├── requirements.txt            # Список зависимостей
│
├── templates/
│   └── index.html             # HTML интерфейс (грузинский)
│
└── static/
    ├── css/                    # CSS стили (пока не нужны, все в HTML)
    └── js/                     # JavaScript (пока не нужен, все в HTML)
```

---

## 💻 КАК ЗАПУСТИТЬ ЛОКАЛЬНО (на вашем компьютере)

### Шаг 1: Установите Python

1. Скачайте Python 3.10 или новее с https://www.python.org/downloads/
2. При установке ОБЯЗАТЕЛЬНО поставьте галочку "Add Python to PATH"
3. Проверьте установку - откройте терминал (cmd в Windows) и напишите:
   ```
   python --version
   ```

### Шаг 2: Скачайте проект

1. Скачайте все файлы проекта в одну папку, например: `C:\university-recommendation\`
2. Также положите туда файл `programs_database.csv`

### Шаг 3: Установите зависимости

1. Откройте терминал/командную строку
2. Перейдите в папку проекта:
   ```
   cd C:\university-recommendation
   ```
3. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```

### Шаг 4: Запустите приложение

1. В терминале запустите:
   ```
   python app.py
   ```
2. Вы увидите что-то вроде:
   ```
   * Running on http://127.0.0.1:5000
   ```
3. Откройте браузер и перейдите по адресу: `http://127.0.0.1:5000`
4. Готово! Сайт работает!

---

## 🌐 КАК РАЗМЕСТИТЬ В ИНТЕРНЕТЕ (БЕСПЛАТНО)

Есть несколько бесплатных вариантов:

### Вариант 1: Render.com (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- Полностью бесплатно
- Автоматическое обновление при изменении кода
- Простая настройка

**Инструкция:**

1. **Зарегистрируйтесь на GitHub**
   - Перейдите на https://github.com
   - Создайте аккаунт (бесплатно)

2. **Загрузите проект на GitHub**
   - Создайте новый репозиторий (New Repository)
   - Назовите его `university-recommendation`
   - Загрузите все файлы проекта

3. **Зарегистрируйтесь на Render**
   - Перейдите на https://render.com
   - Войдите через GitHub аккаунт

4. **Создайте новый Web Service**
   - Нажмите "New +" → "Web Service"
   - Выберите ваш репозиторий `university-recommendation`
   - Настройки:
     - Name: `university-recommendation`
     - Environment: `Python 3`
     - Build Command: `pip install -r requirements.txt`
     - Start Command: `gunicorn app:app`
   - Нажмите "Create Web Service"

5. **Дождитесь деплоя**
   - Render автоматически установит зависимости и запустит сайт
   - Через 2-3 минуты вы получите ссылку типа: `https://university-recommendation.onrender.com`

6. **Готово!**
   - Ваш сайт доступен в интернете
   - Каждый раз при обновлении кода на GitHub, сайт автоматически обновится

### Вариант 2: PythonAnywhere

**Преимущества:**
- Бесплатный план
- Легко обновлять базу данных

**Инструкция:**

1. Зарегистрируйтесь на https://www.pythonanywhere.com (бесплатный аккаунт)
2. Перейдите в "Files" → загрузите все файлы проекта
3. Перейдите в "Web" → "Add a new web app"
4. Выберите "Flask" → Python 3.10
5. В настройках укажите путь к `app.py`
6. Готово! Получите ссылку типа: `https://yourusername.pythonanywhere.com`

---

## 🔧 КАК РАБОТАЕТ СИСТЕМА (технически)

### 1. Фильтрация программ

Когда пользователь выбирает:
- Город → фильтруются только программы в этом городе
- Тип вуза → государственный (бесплатно) или частный (платно)
- Направление → медицина, IT, бизнес и т.д.
- Язык обучения → грузинский, английский, русский

### 2. Расчет совместимости

Для каждой программы считается **взвешенный балл**:

```
Общий балл = (балл1 × коэф1 + балл2 × коэф2 + ...) / (сумма коэффициентов)
```

Где:
- `балл` = балл абитуриента по предмету (0-100%)
- `коэф` = коэффициент важности предмета (из базы данных)

### 3. Проверка минимальных требований

Для каждого предмета проверяется:
```
балл абитуриента > минимальный порог программы
```

Если хотя бы один предмет не проходит → программа помечается как "не подходит"

### 4. Определение шанса поступления

На основе процента совместимости:
- 90%+ → "ძალიან მაღალი" (очень высокий)
- 75-90% → "მაღალი" (высокий)
- 60-75% → "საშუალო" (средний)
- 45-60% → "დაბალი" (низкий)
- <45% → "ძალიან დაბალი" (очень низкий)

---

## 📊 КАК ОБНОВИТЬ БАЗУ ДАННЫХ

Если вы хотите добавить новые программы или обновить информацию:

### Способ 1: Через Excel/Google Sheets

1. Откройте файл `programs_database.csv` в Excel или Google Sheets
2. Добавьте новые строки с программами
3. Убедитесь, что:
   - `program_code` уникален
   - `university_code` соответствует университету
   - Все обязательные поля заполнены
4. Сохраните как CSV (UTF-8)
5. Загрузите обновленный файл на сервер

### Способ 2: Через Python скрипт

Если вам нужно добавить много программ, можно написать скрипт:

```python
import pandas as pd

# Загружаем существующую базу
df = pd.read_csv('programs_database.csv')

# Добавляем новую программу
new_program = {
    'program_code': 99999,
    'university_code': 1,
    'program_name': 'Новая программа',
    'annual_tuition': 2250.0,
    # ... остальные поля
}

# Добавляем в датафрейм
df = df.append(new_program, ignore_index=True)

# Сохраняем
df.to_csv('programs_database.csv', index=False, encoding='utf-8')
```

---

## 🎨 КАК ИЗМЕНИТЬ ДИЗАЙН

Весь дизайн находится в файле `templates/index.html` в секции `<style>`.

**Примеры изменений:**

### Изменить цвета:

Найдите в CSS:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

Замените цвета на свои (используйте https://cssgradient.io):
```css
background: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 100%);
```

### Изменить шрифт:

Найдите:
```css
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Georgian:wght@300;400;600;700&display=swap');
```

Замените на другой шрифт с Google Fonts.

---

## ❓ ЧАСТЫЕ ВОПРОСЫ

### Q: Как добавить новый университет?

**A:** Откройте `universities_info.py` и добавьте университет в словарь `UNIVERSITIES`:

```python
123: {
    "name": "Название университета",
    "name_short": "Краткое название",
    "city": "თბილისი",
    "type": "სახელმწიფო"  # или "კერძო"
}
```

Затем добавьте программы этого университета в `programs_database.csv` с `university_code = 123`.

### Q: Как изменить минимальное количество экзаменов?

**A:** Откройте `app.py`, найдите функцию `get_recommendations` и измените проверку:

```python
if len(other_exams) == 0:  # Здесь 0 - это минимум минус обязательные
```

Замените `0` на нужное число минус 2 (т.к. грузинский и иностранный уже обязательны).

### Q: Как добавить фильтр по стоимости обучения?

**A:** 
1. Добавьте в `index.html` новый select с диапазонами цен
2. В `app.py` добавьте параметр `max_tuition` в функцию `filter_programs`
3. В `recommendation_system.py` добавьте фильтр:
```python
if max_tuition:
    filtered = filtered[filtered['annual_tuition'] <= max_tuition]
```

### Q: Система работает медленно, что делать?

**A:** 
1. Убедитесь, что используете последнюю версию pandas
2. Можно добавить кеширование часто используемых запросов
3. Оптимизируйте базу данных - удалите дубликаты, очистите пустые поля

---

## 🆘 ПОМОЩЬ И ПОДДЕРЖКА

### Если что-то не работает:

1. **Проверьте консоль браузера** (F12 в Chrome/Firefox)
   - Смотрите вкладку "Console" на ошибки JavaScript

2. **Проверьте логи Flask**
   - В терминале, где запущен `python app.py`, будут видны все ошибки

3. **Типичные ошибки:**

   **Ошибка**: `FileNotFoundError: programs_database.csv`
   **Решение**: Проверьте, что файл CSV находится в правильной папке и путь в `app.py` правильный

   **Ошибка**: `ModuleNotFoundError: No module named 'flask'`
   **Решение**: Установите зависимости: `pip install -r requirements.txt`

   **Ошибка**: `UnicodeDecodeError`
   **Решение**: Убедитесь, что CSV файл сохранен в кодировке UTF-8

---

## 📈 ДАЛЬНЕЙШЕЕ РАЗВИТИЕ

Что можно добавить в будущем:

### Простые улучшения:
- [ ] Сохранение результатов в PDF
- [ ] Сравнение нескольких программ
- [ ] Фильтр по количеству мест
- [ ] Сортировка результатов (по цене, по шансам)

### Средние улучшения:
- [ ] Регистрация пользователей
- [ ] Сохранение избранных программ
- [ ] История поисков
- [ ] Email уведомления о сроках подачи документов

### Сложные улучшения:
- [ ] Интеграция с API университетов (если есть)
- [ ] Машинное обучение для более точных прогнозов
- [ ] Чат-бот для помощи
- [ ] Мобильное приложение

---

## 📝 ЗАКЛЮЧЕНИЕ

Поздравляю! Теперь у вас есть полностью рабочая система рекомендаций университетов!

**Что вы получили:**
✅ Веб-сайт с грузинским интерфейсом
✅ Алгоритм подбора программ
✅ Расчет совместимости и шансов поступления
✅ Готовность к размещению в интернете
✅ Полная документация

**Следующие шаги:**
1. Протестируйте систему локально
2. Проверьте работу с разными данными
3. Разместите в интернете (Render.com)
4. Поделитесь с друзьями!

Удачи! 🚀

---

**Автор**: Система создана для помощи абитуриентам Грузии  
**Дата**: Январь 2026  
**Версия**: 1.0
