# 🚀 БЫСТРЫЙ СТАРТ

## Локальный запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Положите файл `programs_database.csv` в корневую папку проекта

3. Запустите приложение:
```bash
python app.py
```

4. Откройте браузер: http://127.0.0.1:5000

---

## Деплой на Render.com (бесплатно)

### Шаг 1: Подготовка

1. Создайте аккаунт на https://github.com
2. Создайте новый репозиторий `university-recommendation`
3. Загрузите в него ВСЕ файлы проекта, включая:
   - app.py
   - recommendation_system.py
   - universities_info.py
   - requirements.txt
   - Procfile
   - .gitignore
   - programs_database.csv (ВАЖНО!)
   - templates/index.html

### Шаг 2: Настройка Render

1. Зайдите на https://render.com
2. Войдите через GitHub
3. Нажмите "New +" → "Web Service"
4. Выберите ваш репозиторий
5. Настройки:
   - **Name**: university-recommendation
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`
6. Нажмите "Create Web Service"

### Шаг 3: Готово!

Через 2-3 минуты ваш сайт будет доступен по ссылке типа:
`https://university-recommendation.onrender.com`

---

## Важные замечания

⚠️ **ОБЯЗАТЕЛЬНО** загрузите файл `programs_database.csv` в репозиторий GitHub!

⚠️ Бесплатный план Render засыпает после 15 минут неактивности. При первом обращении может загружаться 30-60 секунд.

✅ Сайт полностью рабочий и бесплатный!

---

## Обновление данных

Чтобы обновить базу данных:

1. Отредактируйте файл `programs_database.csv` локально
2. Загрузите изменения на GitHub:
```bash
git add programs_database.csv
git commit -m "Обновление базы данных"
git push
```
3. Render автоматически обновит сайт!

---

## Структура проекта

```
university-recommendation/
├── app.py                      # Flask приложение
├── recommendation_system.py    # Логика рекомендаций
├── universities_info.py        # Справочники
├── programs_database.csv       # База данных (ВАЖНО!)
├── requirements.txt            # Зависимости Python
├── Procfile                    # Конфиг для Render
├── .gitignore                  # Исключения для Git
├── README_RU.md               # Полная документация
└── templates/
    └── index.html             # Интерфейс
```

---

## Поддержка

Если возникли проблемы:

1. Проверьте логи в Render (вкладка "Logs")
2. Убедитесь, что все файлы загружены в GitHub
3. Проверьте, что `programs_database.csv` есть в репозитории
4. Прочитайте полную документацию в `README_RU.md`
